{
    "version":"2.0.0",
    "tasks":[
        {
            "label": "Build ROM",
            "type": "shell",
            "command": "mkdir build; ./lib/gbdk/bin/lcc -debug -o build/game.gb main.c",
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}